# Sales copy — use as Stripe Payment Link description / landing page text

## Title
Claude Code Power Pack — Skills, Hooks & Multi-Agent Workflows

## One-liner
6 custom skills, 5 hook recipes, 3 multi-agent workflow scripts, and permission templates for Claude Code — copy-paste and start using in under 10 minutes.

## Description

If you're using Claude Code daily, you've probably re-typed the same instructions a dozen times: "write a good commit message," "don't pad the PR description," "actually verify the test passes." This pack turns those into reusable skills, so you stop re-explaining your standards every session.

**What's inside:**

- **6 custom skills** — changelog-writer, pr-description, commit-message, test-generator, bug-repro, refactor-planner. Each one encodes the actual judgment calls (when to split a commit, when a test is testing nothing, when a "refactor" is secretly a behavior change) — not just a generic prompt template.
- **5 hook recipes** — auto-format on every edit, block edits to lockfiles/CI config, desktop notifications when Claude needs you, a pre-commit test gate, command audit logging. Copy-paste into `.claude/settings.json`.
- **3 multi-agent workflow scripts** — parallel code review with adversarial verification, loop-until-dry bug hunting with a 3-lens judge panel, and a discover→transform→verify migration workflow with worktree isolation so parallel edits never collide.
- **Permission allowlist templates** for Node, Python, and Go projects — a sane starting point instead of a blank `.claude/settings.json`.
- **One-page cheat sheet** covering the permission model, when to reach for a subagent vs. a full workflow, and CLAUDE.md hygiene.

Built by someone who uses Claude Code for actual daily work, not a prompt-pack reseller. No fluff, no padding — every file is something you'd actually drop into a real project.

**Format:** zip download, works with any Claude Code install. Personal/team use.

**Price:** $15 one-time.

## Short version (for a tweet/listing card)

Claude Code Power Pack: 6 skills, 5 hooks, 3 multi-agent workflows, permission templates. Copy-paste, start using today. $15.
